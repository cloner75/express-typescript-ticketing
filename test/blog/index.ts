// test modules
import { equal } from "assert";

class BlogUnitTest {
  static start() {
    describe('*** Blog Module ***', () => {
      before(() => {
      });

      it('🚀 ~ Blog ~ Create', () => {
        equal(1, true);
      });


      it('🚀 ~ Blog ~ Update', () => {
        equal(1, true);
      });


      it('🚀 ~ Blog ~ Find', () => {
        equal(1, true);
      });


      it('🚀 ~ Blog ~ Find ~ One', () => {
        equal(1, true);
      });


      it('🚀 ~ Blog ~ Remove', () => {
        equal(1, true);
      });

      after(() => {
      });

    });
  }
}
export default BlogUnitTest;